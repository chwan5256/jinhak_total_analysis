# 변경 이력

버전은 `index.html`의 `VERSION` 상수가 기준입니다. `scripts/release.sh`가 자동으로 갱신합니다.

## ver.2026.8 — 2026-09-21

**목표 학과를 계열 단위로 다시 짜서 파일로 떼어냈고, 면접 대비에 답안 초안을 넣었다.**

### 목표 학과 — 12개 → 42개 학과군, 6개 계열

- 기존 12개는 공학·자연에 치우쳐 있었고 **건축·토목이 아예 없었다.** 자연·사회·인문·어문도 한 덩어리로 뭉쳐 있어 학생별 진단이 뭉개졌다.
- 셀렉트를 **계열(optgroup) → 학과군(option)** 2단으로 바꿨다. 계열은 공학 / 자연과학 / 의약·보건 / 사회과학 / 인문·어문 / 사범·교육 6개.
- 새로 들어온 학과군: **건축학(5년제)·건축공학·실내건축**, **토목·도시공학·교통**, 항공우주·조선해양, 전기·에너지시스템·원자력, 환경공학·안전공학, 생명공학·의공학, 산업공학, 물리학, 화학, 생명과학, 천문우주·에너지자원, 식품영양, 농생명, 약학, 수의예, 간호, 보건계열, 행정·정치외교, 법학·경찰행정, 사회학·사회복지, 심리학, 지리·관광, 국어국문, 영어영문, 제2외국어문, 사학·철학, 문화콘텐츠·문헌정보, 초등교육(교대), 수학·과학교육, 국어·영어·사회교육.
- 기존 학과군도 평가 포인트가 섞여 있던 것을 분리했다 — 화학공학 ↔ 신소재, 경영 ↔ 경제·무역, 생명과학 ↔ 약학 ↔ 의예.
- 학과군마다 **필수·권장 이수 과목 / 핵심 키워드 / 사정관 평가 포인트 / 확인 문항 4개**를 새로 썼다.

### 면접 대비 — 자기소개 · 지원동기 답안 초안

- 면접 화면 맨 위에 **답안 초안** 구역을 신설했다. 문항 2종(자기소개·지원동기) × 구성안 3종 × 분량 4종(500·1000·1500·2000자).
- **재료는 전부 이 학생의 생활기록부 문장이다.** 없는 경험은 만들어 넣지 않는다. 기록이 얇아 목표 분량에 못 미치면 "생기부에서 더 뽑아 쓸 문장이 부족합니다"라고 화면에 그대로 적는다.
- 개조식 서술(`~함`, `~하였음`, `~지님`)을 **1인칭 구술체로 기계 변환**한다. 받침·모음 축약을 계산해 `지님 → 지녔습니다`, `만듦 → 만들었습니다`까지 처리한다. 그래도 어색한 문장은 남으므로, 학생이 본인의 말로 다시 쓰는 것을 전제로 한다.
- **문장마다 출처 [n]을 붙인다.** 하단 목록에 `3학년 · 세특 · 물리학Ⅱ — "원문…"` 형태로 어느 기록에서 나왔는지 표시한다. 출처가 없는 문장은 연결을 위한 구성 문장이다.
- 구성안은 자기소개 = 탐구 축 / 문제해결·성장 / 공동체·협업, 지원동기 = 교과 수업→탐구 확장 / 사회 문제→전공 필요성 / 진로 탐색→학과 선택.
- 3년 축은 **연결망 분석에서 학년·영역을 가로지른 개념어**를 그대로 가져온다.
- 공백 포함·제외 글자 수를 함께 표시한다. `이 초안 복사`(현재 안), `전체 세트 복사`(2문항 × 3안 × 4분량 = 24종).
- 머리말의 "모범답안을 주지 않습니다"를 **"초안은 드리되, 모범답안은 아닙니다"**로 고쳤다. 답을 주지 않는다고 써놓고 주는 것은 앞뒤가 맞지 않는다.

### 학과 데이터를 index.html 에서 떼어냈다

- 42개로 늘어난 학과 표준을 **`data/majors.json`** 으로 옮겼다. 학과 추가·수정은 이제 JSON 한 곳에서만 한다.
- **오프라인 축약본.** `file://` 로 열거나 학교망이 막으면 fetch 가 실패한다. 그때 셀렉트가 비면 전공 진단·이수 점검·면접 문항이 통째로 죽으므로, 계열별 대표 6개를 시드로 남겨 앱이 반쪽이라도 돌게 했다. 화면 상단에 **"계열 대표 6개 축약본으로 동작 중"** 이라고 그대로 밝힌다. 숨기지 않는다.
- 시드는 손으로 고치지 않는다. **`scripts/build_majors.py`** 가 JSON 에서 다시 찍는다. 스크립트는 검증도 함께 한다 — 키 형식, group 소속, 이름 중복, 배열 타입, 시드 대상 존재, 계열 누락, 로마숫자 과목명.
- JSON 이 늦게 도착해도 이미 떠 있는 분석 화면의 전공 구역은 자동으로 다시 그려진다. 고른 학과는 그대로 유지된다.
- **배포 전 검사 ⑤ 신설.** `build_majors.py --check` 통과 + 시드가 JSON 과 어긋나지 않았는지 `git diff` 로 확인한다. 어긋난 채 배포되면 오프라인 축약본이 거짓말을 하게 된다.
- 단일 파일 원칙은 유지된다. 떼어낸 것은 `.json` 데이터이지 `.js` 코드가 아니다.

### 그 밖

- 내비게이터(면접)에 **답안 초안** 추가.
- 학생 자료 지우기에 초안 구역과 근거 풀 캐시를 포함했다. 교사가 문장 분류를 수정하면 초안도 다시 만들어진다.
- 범용 클립보드 복사 헬퍼(`Export._copy`)를 추가해 복사 성공 문구를 호출부가 정하도록 했다.

## ver.2026.7 — 2026-09-10

**버전 체계를 바로잡았다.** 프로그램 버전은 `ver.2026.x`, 페이지 주제는 **2027학년도 대입전형**이다.
ver.2027.0~2027.3으로 붙였던 것은 학년도와 버전을 혼동한 것이라 2026.7로 되돌린다.

### 계열 판별 신설

- 이 학생이 **실제로 어느 계열의 활동을 했는지** 13개 계열로 판별한다 — 기계·로봇·모빌리티 / 전기·전자·반도체 / 컴퓨터·인공지능·데이터 / 화학·신소재 / 생명·의약 / 지구·환경·에너지 / 수학·통계 / 건축·토목 / 경영·경제 / 사회·정치·법 / 어문·문학 / 교육·심리 / 예술·디자인·체육.
- 상위 두 계열의 집중도로 **"뚜렷하게 모였다 / 폭이 넓다 / 어디로도 모이지 않았다"**를 판정한다.
- 실측: 지구·환경·에너지 27% / 전기·전자·반도체 17% — 상위 둘 45%.

### 탐구 완결성을 '줄기'로 다시 만들었다

- 활동을 하나하나 나열하던 것을 없앴다. 그건 생기부 원본을 보는 것과 다르지 않다.
- **한 주제가 어느 교과에서 시작해 어느 활동으로 번졌는지를 한 줄로 잇는다.**
  개별 교과가 혼자 다섯 단계를 갖출 필요는 없고, **줄기 전체에서 닫히면** 된다.
- 실측 결과가 이렇게 읽힌다:
  > **에너지** [A 심화탐구] 교과 13 · 영역 4 · 3개 학년
  > 1학년 교과(국어/수학) → 2학년 교과(독서/수학Ⅰ) → 2학년 자율 → 2학년 동아리 → 2학년 진로 → 3학년 교과(화법과작문/심화수학Ⅰ) → 3학년 동아리
- 줄기 자격을 좁게 잡았다. 두 과목 이상 + 교과 밖 활동, 또는 3년 관통 + 두 영역. 두 글자 주제어는 계열 사전에 실제로 있는 용어일 때만 인정한다("전력"·"환경"은 남고 "스스"·"미치"는 버려진다).
- 실측: 이어진 줄기 19개 / 3년 관통 8개 / 닫힌 줄기 5개.
- 기존 '활동 핵심요소 연결망'은 이 구역에 흡수했다.

### 봉사활동 · 독서활동 특기사항 제외

- 상급학교에 제공되지 않는 항목이다. 파싱 직후 분석 대상에서 뺀다. 문장 분석 필터에서도 없앴다.

### 배치와 조작

- 구역 순서: **자질 판단 → 전공 연계·이수 → 탐구 줄기 → 서류 경쟁력 → 교과·성적 → 역량 흐름 → 문장 분석 → 상담 시트.**
  전공 연계성·교과 이수 점검을 자질 판단 바로 다음으로 올렸다.
- 서류 경쟁력의 '이 판정의 근거 문장'을 **기본으로 펼쳐 두고**, 누르면 접히게 했다.

## ver.2027.3 — 2026-09-10

**실제 PDF를 올려주셔서 진짜 원인을 찾았다.** 그리고 양적 판단을 완전히 걷어냈다.

### 파싱 — PDF 경로가 통째로 깨져 있었다

- **[근본 원인] 본문의 공백이 전부 사라지고 있었다.** 이 PDF는 폰트에 공백 ToUnicode 매핑이 없어서 스페이스가 **U+0001 제어문자**로 나온다. 실측 **5,666개**. 그래서 `"수학이란과목을필수적으로공부하면서도..."` 처럼 본문이 통째로 붙었고, 화면에는 네모·괄호 비슷한 글자(『 』)로 보였다. 문장 분리·역량 분류·주제 연결망·탐구 판정이 전부 여기서 무너졌다. 제어문자를 공백으로 되돌린다.
- **셀이 붙었다.** pdf.js는 글자 하나를 항목 하나로 준다. 기존 reflow는 그냥 이어붙여 `수학수학Ⅰ`(교과명+과목명)이 되었고 과목 대조가 실패했다. `item.width`로 실제 간격을 재서 칸 구분(공백 2개)과 어절 구분(공백 1개)을 넣는다.
- **[학교망에서 멈춤] cMapUrl·standardFontDataUrl이 외부 CDN을 물고 있었다.** CDN이 막힌 망에서 `getDocument`가 영원히 끝나지 않아 진행률 15%에서 아무 말 없이 멈췄다. 실측상 이 PDF는 cMap 없이도 한글이 온전히 추출되므로 걷어냈다. 60초 감시 타이머와 안내를 붙였다.
- **실측 결과 — 실제 톡톡스쿨 PDF 업로드:** 톡톡스쿨 자동 감지 ✓ / 학생명 인식 ✓ / **이수 과목 34건 학년 전부 정확** ✓ / 잡음 문장 0건 ✓ / 콘솔 오류 0건 ✓

### 정성 판단 — 양은 화면에서 사라졌다

- **문장 수·비중(%)·차이(%p)를 전부 걷어냈다.** 상대 비교도 하지 않는다. 이 기록 하나만 놓고 절대 등급(**A 심화탐구 / B 탐구 / C 기술 / D 나열**)만 말한다. 같은 내신대와의 비교는 교사가 직접 하는 일이다.
- **탐구 완결성 구역 신설.** 활동 하나하나에 대해 **계기 → 방법 → 결과 → 한계 → 확장** 중 무엇이 있고 **무엇이 빠졌는지**를 보여준다. 결과물과 느낀 점은 각각 0.5단계로 센다. 학년·영역·과목명을 함께 붙여 어느 수업의 어떤 활동인지 바로 보이게 했다.
- **자질 축 판별 (개요 자리).** 학업역량 / **진로연계·계열적합도** / 공동체(인성) 셋을 같은 잣대로 재고 무게중심을 짚는다. 계열적합도는 서술만으로 볼 수 없으므로 **핵심과목 이수율·위계(Ⅰ→Ⅱ)·전공 관련 과목 평균 등급**을 함께 반영한다.
- **실측:** 학업 30(C) / 진로연계 59(B, 핵심과목 5/5·위계 2/2) / 공동체 55(B). 닫힌 탐구 7건. → "진로 쪽이 축, 학업이 가장 얇음, 격차 29점"

### 그 밖

- 시간 기반 자동 삭제 제거(요청). 지우기는 버튼으로만.
- 내비게이터에 '탐구 완결성' 추가.

## ver.2027.2 — 2026-09-10

**"양으로 판단하는 것 아니냐"는 지적이 맞았다.** 그리고 그 위에 올린 진단보다 먼저, **파싱이 깨져 있었다.**

### 파싱 — 토대부터 다시

- **[치명적] 이수 과목의 학년이 어긋났다.** 일본어Ⅰ·체육이 1학년인데 2학년으로, 생명과학Ⅰ·생활과과학·지구과학Ⅰ이 2학년인데 3학년으로 밀렸다. 원인은 맨 앞 '교과 성적' 요약표를 읽은 것 — 교과명·과목명 셀이 모두 rowspan이라 엉킨다.
  **'교과 활동 XXX' 별 소표로 소스를 바꿨다.** 교과명이 제목에 있고 행이 적어 훨씬 정확하다.
- **학년 라벨 복원을 두 단계로.** ① 생기부는 3학년 1학기에 마감하므로 **2학기 칸이 통째로 빈 채 표 끝에 몰린 과목은 3학년**이다. 라벨 위치보다 이 신호가 세다. ② 남은 구간은 rowspan 라벨이 블록 세로 중앙에 찍히는 규칙을 역산한다(`e = 2L − s + 1`). **실측: 34과목 전부 학년 일치.**
- **『 』「 」 같은 괘선·괄호 기호 제거.** 표 머리글이 활동으로 잡히던 것('국어 교과 활동' 등)과 어절 중간에서 잘린 조각 문장('더십을 보임.', '체적으로 설명함.')도 걸러낸다. **실측: 이상 추출 6건 → 0건.**

### 정성 진단 — 문장 수를 세지 않는다

- **탐구 완결성이 핵심 지표.** 계기 → 방법 → 결과 → 한계 → 확장. 다섯 단계 중 몇 개가 한 활동 안에 닫히는가. 활동 나열과 심층 탐구는 개수가 아니라 이것으로 갈린다.
- **역량마다 잣대가 다르다.** 학업·진로는 탐구 완결성 + 자기주도 표현 + 구체성(수치·이론명), 공동체는 대상 구체성 + 역할 명시 + 3년 지속 + 갈등 조율. 상투어("성실히 참여함")는 감점한다.
- **활동을 통째로 한 역량에 몰아주지 않는다.** 그러면 공동체역량이 사라진다 — 공동체 서술은 세특·자율 안에 흩어져 있지 따로 서지 않는다. 그 역량의 문장이 2개 이상 든 활동을 그 역량의 활동으로 세고, 완결성도 그 역량 문장만 모아서 본다.
- **판정 기준선이 비중에 따라 올라간다.** 50%를 보는 요소에 정성 40점을 내미는 것과 10%짜리에 40점인 것은 다르다. `기준 = 35 + (비중 − 20) × 0.5`.
- **실측으로 그림이 뒤집혔다.** 이 학생은 진로 문장이 31%(91개)로 많지만 26활동 중 심층은 1건 — 정성 26점. 반대로 공동체는 문장 17개(6%)뿐이지만 구체적 대상 9건·역할 명시가 있어 59점. **양으로 보면 공동체가 약점, 질로 보면 강점이다.**

### 전형 적합도 순위

- 수록 24개 전형에 이 학생부를 전부 대조해 **어느 잣대에서 이 기록이 살아나는지**를 순위로 보여준다. 실측: 가천대(인성 40%) 41점 ↔ 숭실대 서류형(탐구역량 60%) 32점. 합격 가능성 순위가 아니라는 것을 화면에 명시한다.
- 지수에서 '양'을 뺐다. 양을 섞으면 질 신호를 상쇄해 어느 전형이나 47~50으로 뭉개졌다.

### 평가요소 데이터 확대 — 11 → 18개 대학

- 가이드북이 없는 대학은 **2027 수시모집요강**에서 뽑았다. 세종대·아주대·명지대·한성대·삼육대·한국공학대·가천대 추가. 총 **27전형**.
- 가천대는 표준 3역량이 아니다 — **인성 40 / 진학의지 40 / 학업역량 20**. 다른 대학 기준으로 공동체가 얇게 나온 학생은 여기서 특히 불리하다.
- 세종대는 창의융합역량 체계이고 면접형(30:50:20)과 서류형(50:30:20)이 정반대다.

### 그 밖

- **자동 삭제 타이머(선택).** 10·20·30·60분 조작이 없으면 먼저 묻고 30초 뒤 지운다. 기본은 꺼짐. 설정값만 브라우저에 남고 학생 자료는 남지 않는다.
- 면접 화면 해시(`#interview`) 라우팅 — 뒤로가기·앞으로가기 동작.
- 평가요소 생성기 `scripts/build_criteria.py` 갱신.

## ver.2027.1 — 2026-09-10

**이 도구의 정체를 바로잡았다.** 면접 질문 생성기가 아니라 **학생부종합전형 서류 경쟁력 진단기**다.
학종은 서류 100% 또는 서류+면접으로 뽑고, 서류 평가요소의 비중은 대학마다 다르다. 같은 학생부라도 진로역량 50%인 전형과 학업역량 50%인 전형에서 전혀 다르게 읽힌다. 그 차이를 드러내는 것이 이 도구의 일이다.

- **서류 경쟁력 진단 구역 신설 (핵심).** 11개 대학 19개 전형의 **실제 서류 평가요소와 반영비율**을 각 대학 2027 학종 가이드북에서 옮겨 `data/criteria.json`으로 만들었다. 대학·전형을 고르면 그 비중으로 학생의 근거를 다시 계산한다.
  - 요소마다 **수요(대학 반영비율) vs 공급(학생 근거 비중)** 을 이중 막대로 겹쳐 보여주고, 차이를 %p로 낸다.
  - 근거는 문장 수만 세지 않는다. 분류 신뢰도·**심화 서술 비율**·성찰 표현·영역 확산을 합쳐 **근거 밀도(0~100)** 를 낸다. 학업역량에는 교내 표준화 점수(z)를 반영한다.
  - **비중 가중 근거 두께 지수**를 내되, 합격 가능성이 아님을 화면에 명시한다. 같은 학생이 전형을 바꾸면 값이 달라지는 것을 보라고 만든 숫자다.
  - 실측: 이 학생은 숭실대 면접형 63 / 광운대 면접형 59 / 가톨릭대 68. 학업역량 62%가 강점인 학생이라 진로역량 50%짜리 전형에서 값이 떨어진다.
- **대학군 표기 제거.** 군은 내부 키로만 쓴다. 공시 자료가 없는 대학은 **"○○대의 공시 자료가 없습니다. 비슷한 수준의 △△대 기준으로 대신 대조합니다"** 로 안내하고, 정확한 비중이 아니라 참고임을 적는다. 대학 선택지는 *공시 자료 수록* / *비슷한 수준 대학 기준으로 대신함* 두 그룹으로 나눈다.
- **활동 핵심요소 연결망 신설.** 평가자는 활동을 개수로 세지 않고 **이어지는지**로 본다. 생기부 전체에서 주제어를 뽑아 학년 × 영역(세특·자율·동아리·진로·봉사·독서·행특) 확산 지도를 그리고, **3년 연계 / 영역 확산 / 한 영역에만 머문 주제**를 구분한다. 형태소 분석기 없이 조사·어미 절단 + 불용어 + 부분문자열 중복 제거로 개념어만 남긴다.
- **면접 대비를 별도 화면으로 분리.** 서류 진단 맨 아래 **면접 대비로 넘어가기** 버튼으로 이동한다. 서류 100% 전형이면 그 사실을 적고 여기서 끝내도 된다고 말한다. 화면 전환은 페이지 이동이 아니라 뷰 교체다 — 새 페이지로 넘기면 분석 결과를 어딘가에 저장해야 하는데, 그것이 이 프로젝트의 첫 번째 원칙과 충돌하기 때문이다.
- **공통 문항 7종 추가.** 자기소개·지원동기·대표활동·역할확인·문제해결·학업계획·마무리는 대학을 가리지 않는다. 각 문항에 이 학생의 생기부에서 뽑은 근거를 붙이고, 자기소개에는 연결망에서 찾은 **3년 축 후보**를 제시한다.
- **서류 진단과 면접의 연결.** 서류에서 얇게 나온 요소는 면접에서도 답이 얇아진다. 면접 화면에 그 경고를 띄운다.
- **인쇄 범위를 '지금 보고 있는 화면'으로.** 서류 화면에서 인쇄하면 서류 결과만, 면접 화면에서 인쇄하면 면접 자료만 나온다. PDF 저장도 같은 범위다.
- 만든이 표기를 **남강고등학교 진로진학부**로 바꾸고 버전을 함께 적는다.
- 상담 요약 시트에 선택한 전형과의 대조 한 줄을 넣는다.
- **[배포 실패 수정]** GitHub Actions의 문법 검사가 `'<script>\n"use strict";'` 를 **글자 그대로** 찾고 있었다. 조립 과정에서 빈 줄 하나가 끼어 `<script>\n\n"use strict";` 가 되자 "앱 스크립트 블록을 찾지 못했습니다"로 검사가 실패했고, ver.2027.0부터 배포가 통째로 막혀 있었다. 조립기가 앞뒤 공백을 정리하도록 고치고, **검사 자체도 공백에 관대한 정규식으로 바꿨다.** 빈 줄 하나가 릴리스를 막는 게이트는 게이트가 아니라 지뢰다. `data/*.json` JSON 문법 검사도 추가했다.
- **학생 자료 지우기 버튼 신설.** 서류·면접 두 화면 액션바에 모두 둔다. 확인 대화상자에서 학생 이름을 보여주고 "내려받지 않은 결과는 복구할 수 없습니다"라고 경고한 뒤 지운다.
- **[숨은 결함 수정] '새 분석'은 감추기만 했지 지우지 않았다.** `#result-dashboard`를 `hidden` 처리할 뿐, 그 안에는 학생 이름·300문장·성적표가 그대로 남아 있었다(실측 419KB). 뒤로가기·개발자도구·인쇄 미리보기로 다시 보였다. 이제 `state`를 비우는 것에 더해 **DOM 의 내용까지 실제로 지운다** — 결과 컨테이너 30곳 innerHTML 비우기, 학생 정보 텍스트 20곳 초기 문구로 복원, 막대 그래프 0%, 입력창·필터·검색어 초기화, 해시 제거. 확인 대화상자에 남는 학생 이름까지 지운다. 실측: 지운 뒤 문서 전체에서 학생 이름 0건, `state.activities` 0건.
- **면접 화면에 주소를 붙였다.** `#interview` 해시로 전환해 **뒤로가기·앞으로가기가 동작**한다. 별도 `.html` 파일로 나누지 않은 이유는 아래에 적는다.

> **왜 면접을 별도 파일로 나누지 않았나**
> 파일을 나누면 분석 결과를 `sessionStorage` 같은 곳에 저장해 넘겨야 한다. sessionStorage는 브라우저 밖으로 나가지 않으므로 "학생 자료는 브라우저를 떠나지 않는다"는 원칙 자체를 깨지는 않는다. 문제는 두 번째 약속이다 — 이 페이지는 **"새로고침하면 즉시 사라집니다"** 라고 적어 두었고, sessionStorage는 새로고침을 견디고 탭을 닫을 때까지 남는다. 교무실 공용 PC에서 상담을 마치고 탭을 안 닫으면 다음 사람이 뒤로가기로 학생 기록을 볼 수 있다. 해시 전환은 주소창과 뒤로가기라는 실익은 그대로 주면서 데이터는 메모리에만 두므로, 새로고침 한 번에 사라진다. 실측으로 확인했다 — 새로고침 후 `sessionStorage` 0개, `localStorage` 0개, 남은 데이터 0건.

- 평가요소 데이터 생성기 `scripts/build_criteria.py` 추가. 확인하지 못한 비중은 **추정하지 않고** null로 두고 화면에 "확인 필요"로 표시한다.

## ver.2027.0 — 2026-09-09

**「생기부 렌즈 · Record Lens」** 로 이름과 껍데기를 새로 지었다. 만든이·저작권 표기를 넣고, 사용법·개인정보 처리·알려진 한계를 안내 모달로 분리했다.

- **면접 준비 구역 신설.** 대학이 법정 공시하는 **선행학습 영향평가 자체평가 보고서**에서 실제 출제된 면접 문항 44건을 뽑아 `data/exams.json`으로 정규화했다. 생성한 문항이 아니라 실제 기출이다. 학생 생기부에 등장하는 어휘(가중치 2)와 검증 대상 영역의 일치(가중치 1)로 점수를 매겨 상위 20건을 보여주고, **그 질문에 쓸 근거 문장을 학생 생기부에서 찾아 붙인다.**
- **모범답안은 주지 않는다.** 외운 답은 꼬리 질문에서 무너지고, 면접관이 확인하는 것은 학생부에 적힌 본인의 경험이다. 답을 써주는 대신 답변 설계 틀(상황–행동–결과–성찰)과 근거 위치를 준다.
- **대학군 3군 체계.** 기출은 반드시 **어느 군의 어느 대학** 문항인지 함께 표기한다. 자료가 없는 군은 선택지에 "자료 없음"으로 표시하고, 없는 대로 넘어간다. 현재 수록은 2군(동국대)뿐이다.
- **범위를 학생부종합전형으로 한정.** 논술 기출은 추출 단계에서 제외한다. 학종은 서류 100%와 서류+면접으로 나뉘므로 문항마다 전형 유형을 명시한다.
- **미이수 핵심과목 경고.** 대학은 미이수 사유를 실제로 묻는다(2군·동국대: "물리학Ⅰ을 이수하고 물리학Ⅱ는 이수하지 않았습니다. 이수하지 않은 이유가 있다면 설명해 주세요"). 성적표 대조로 미이수가 잡히면 그 기출을 근거로 경고를 띄운다.
- **내비게이터 추가.** 개요 · 교과·성적 · 역량 흐름 · 전공 진단 · 문장 분석 · 면접 준비 · 상담 시트 7구역을 스크롤 스파이와 함께 상단에 고정한다. 분석 전에는 감춘다.
- **내보내기 4종 + 메일.** CSV(문장별 원자료) · **엑셀**(성적표까지 포함, 라이브러리 없이 HTML 표를 `.xls`로) · PDF(인쇄의 'PDF로 저장') · 인쇄 · 메일(요약을 `mailto:` 본문으로. 전송하지 않고 기본 메일 앱을 열 뿐이다).
- **인쇄 범위를 결과로 한정.** 헤더·푸터·업로드 화면·액션바·필터바·모달·토스트·부록을 전부 걷어내고 `#result-dashboard`만 남긴다. PDF 저장도 인쇄와 같은 범위다. 인쇄 전용 머리글(학생·문장 수·출력일·원본 파일명)과 꼬리말(만든이)을 붙였다.
- **상담 시트의 미이수 판정을 성적표 기준으로 교체.** 세특 본문 문자열 검색이 남아 있어 "미적분을 활용하여"를 이수로 오판하던 마지막 경로를 없앴다.
- 안내 모달 3종: 사용법 / 개인정보 처리·유의사항 / 알려진 한계. 규칙 기반 분류의 천장(실측 63%)과 PDF 서식 오차를 숨기지 않고 적었다.
- 입시결과 대조 패널은 부록(`<details>`)으로 내렸다. 이 도구의 주제는 **생기부 분석**이지 지원 판단이 아니다.
- 기출 추출기 `scripts/build_exams.py` 추가. 좌표 기반 다단 복원기 `scripts/pdfcols.py` 추가.

## ver.2026.6 — 2026-09-08

- **비슷한 내신대 비교 신설.** 전년도 대학 발표 입시결과 558건(5개 대학)을 `data/admissions.json`으로 정규화하고, 학생 평균 내신과 대조해 모집단위별 **여유·적정·도전** 스펙트럼을 보여준다. 전형 유형 필터와 대학·학과 검색을 붙였다.
- **교과별 등급 직접 대조.** 합격선이 교과별(국어·수학·영어·과학)로 공개된 자료는 학생의 교과별 등급과 1:1로 비교해 과목별 우위·열위를 색으로 표시한다. 전체 평균만 보면 "적정"이어도 특정 교과가 부족한 경우를 잡아낸다.
- 대학마다 컷 정의가 다르다(평균 / 50%컷 / 70%컷 / 85%컷 / 최고). **없는 값은 채우지 않는다.** 비교 기준은 평균 → 50%컷 → 85%컷 순으로 대체한다.
- 데이터 파일은 있으면 쓰고 없으면 조용히 넘어간다. `file://`로 열거나 파일이 없어도 앱은 완전히 동작한다.
- 입시결과 원자료 변환기 `scripts/build_admissions.py` 추가. 참고자료는 저장소 밖(`C:\repos\context`)에 두고 산출물만 커밋한다.
## ver.2026.5 — 2026-09-08

- **톡톡스쿨 '학생부현황' PDF 지원.** 나이스 생기부II는 한컴 PDF로 출력되는 이미지 스캔본이라 텍스트가 추출되지 않는다(실측 16자). 톡톡스쿨 파일은 Chrome 인쇄본이라 5만 자 이상이 온전히 나오고, **이수 과목·단위수·원점수·평균·표준편차·등급까지** 들어 있다. 업로드 시 자동 감지한다.
- **[치명적 버그 수정] 전공 이수 점검표의 유니코드 불일치.** 코드는 `수학I`(U+0049 라틴 대문자 I), 문서는 `수학Ⅰ`(U+2160 로마 숫자)를 쓴다. 서로 다른 문자라 `includes()`가 영원히 실패했고, 수학Ⅰ·Ⅱ, 물리학Ⅰ·Ⅱ, 지구과학Ⅰ·Ⅱ가 전부 "미이수"로 표시됐다. 과목명 정규화(`normCourse`)를 도입해 로마숫자·라틴자·공백·중점을 통일한다.
- **이수 판정 근거를 성적표로 교체.** 기존에는 세특 본문 전체에서 과목명을 문자열 검색했다. 세특에 "미적분을 활용하여"라고만 적혀도 이수로 오판했다. 이제 성적표의 이수 과목 목록과 대조하고, 성적표가 없을 때만 본문 언급을 약한 근거(❓)로 표시해 그 사실을 드러낸다.
- **2015 개정 ↔ 2022 개정 동등 과목 사전 추가.** 물리학Ⅱ ↔ 역학과에너지·전자기와양자, 지구과학Ⅱ ↔ 지구시스템과학·행성우주과학, 수학Ⅰ ↔ 대수, 미적분 ↔ 심화수학Ⅰ 등을 상호 인정한다.
- **교과 성적 프로파일 신설.** 평균 내신 등급, 교과별 등급, 그리고 원점수·평균·표준편차로 산출한 **표준화 점수(z)와 교내 상위 백분율**을 보여준다. 등급은 9단계로 뭉개지지만 z는 연속적이라 같은 3등급 안에서의 위치가 드러난다. 전 과목 z와 수학·과학 z를 나눠 계열 강점을 판별한다.
- 전공 표준 데이터베이스에 지구환경과학·수학통계·의약학·사범 계열 반영(총 12종).
- 성적표의 학년 셀은 rowspan으로 블록 중앙에만 찍혀 이월 방식으로는 어긋난다. 라벨 위치의 중점을 경계로 되돌리고, 1학년 공통과목(통합사회·통합과학·과학탐구실험)을 앵커로 확정한다.
- 분류 규칙에 독서 연계 탐구·산출물 작성·비판적 논증 계열을 보강. 실제 생기부 300문장 기준 규칙 미적중이 128건 → 110건으로 감소.
- 신뢰도 산식에서 길이 정규화를 분리. 서술이 긴 실제 생기부에서 신뢰도가 일괄로 깎이던 문제를 해소했다.
- 저신뢰 라벨의 의미를 "분류 실패"가 아니라 **"역량 근거가 약한 서술"**로 재정의. 학생부 보완이 필요한 지점을 짚는 진단 지표로 쓴다.
- `.gitignore`에 `_context/` `_private/` `_students/` 및 hwp·xlsx·csv 차단 추가. 이 저장소는 공개이므로 학생 자료가 흘러들어가지 않도록 한다.
## ver.2026.4 — 2026-09-06

- **[치명적 버그 수정]** PDF 텍스트 추출이 한 페이지를 한 줄로 이어붙여, 실제 나이스 PDF를 넣으면 전체가 하나의 활동으로 뭉개지던 문제. pdf.js `transform` 좌표로 줄을 복원하도록 교체했습니다. 샘플 데이터에서만 정상 동작하던 이유였습니다.
- 한 줄로 뭉친 OCR·복붙 텍스트에도 영역 경계를 되살리는 `Parse.reflow()` 추가
- 분류 체계를 학종 공통 평가요소 **3역량 10항목**으로 재설계 (기존: 3역량 평면 분류)
- 분류기를 단순 키워드 카운트에서 **가중 정규식 규칙 + 영역 사전확률 + 길이 정규화**로 교체하고, 문장별 신뢰도를 산출합니다
- 나이스 상투어("성실히 참여함") 억제 규칙 추가 — 근거 없는 문장을 학업역량으로 밀어넣던 편향 제거
- 신뢰도 60% 미만 문장에 재검토 표시, "재검토 필요만 보기" 필터 추가
- 학년별 역량 흐름 막대와 **위계성 진단 코멘트** 추가
- 1:1 상담용 요약 시트 및 텍스트 복사 기능 추가
- 되돌리기(Ctrl+Z), 키보드 재분류(1/2/3), 포커스 표시 등 접근성 보강
- A4 인쇄 레이아웃 정비 (`@page`, 페이지 나눔, 인쇄 전용 머리글)
- 전공 표준 데이터베이스 8종 → **12종** (지구환경·수학통계·의약학·사범 계열 추가)
- CSV에 세부항목·신뢰도·교사수정 여부 열 추가
- 학교망 CDN 차단 시 페이지 전체가 죽던 문제 방어
- 전역 변수를 제거하고 단일 `App` 모듈(IIFE)로 재구성 — 이후 기능 추가 시 충돌 방지
- 봉사활동 영역 인식 및 나이스 머리글·페이지번호 노이즈 제거

## ver.2026.3

- 최초 공개 버전 (PDF 파싱, OCR 폴백, 3역량 분류, 드래그 재배치, 전공 진단, CSV)
